-- Carry People Simulator 3

-- Identifier le jeu actuel via son ID
local placeId = game.PlaceId

if placeId == 7267631004 then  -- ID de "Carry People Simulator 3"
    -- Exécuter le script principal pour "Carry People Simulator 3"
    print("Vous êtes dans Carry People Simulator 3. Chargement des scripts...")
    
    -- Charger et exécuter le premier script
    local success1, error1 = pcall(function()
        loadstring(game:HttpGet('https://pastebin.com/raw/YSL3xKYU'))()
    end)
    -- Vérification du premier script
    if success1 then
        print("Premier script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du premier script : ", error1)
    end

    -- Charger et exécuter le deuxième script
    local success2, error2 = pcall(function()
        loadstring(game:HttpGet('https://rawscripts.net/raw/Universal-Script-Anti-kick-11846'))()
    end)
    -- Vérification du deuxième script
    if success2 then
        print("Deuxième script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du deuxième script : ", error2)
    end

-- Build A Boat For Treasure

elseif placeId == 537413528 then  -- ID de "Build A Boat For Treasure"
    -- Exécuter le script principal pour "Build A Boat For Treasure"
    print("Vous êtes dans Build A Boat For Treasure. Chargement des scripts...")

    -- Charger et exécuter le premier script
    local success1, error1 = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/max2007killer/auto-build-not-limit/main/autobuildsupportkrampus.txt", true))()
    end)
    -- Vérification du premier script
    if success1 then
        print("Premier script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du premier script : ", error1)
    end

    -- Charger et exécuter le deuxième script
    local success2, error2 = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Youssef1235o/BuildBoatGui/main/Gui-Build-Boat.txt"))()
    end)
    -- Vérification du deuxième script
    if success2 then
        print("Deuxième script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du deuxième script : ", error2)
    end

-- Prison Life

elseif placeId == 155615604 then  -- ID de "Prison Life"
    -- Exécuter le script principal pour "Prison Life"
    print("Vous êtes dans Prison Life. Chargement des scripts...")

    -- Charger et exécuter le premier script
    local success1, error1 = pcall(function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/elliexpln/PrizzLife/main/pladmin.lua'))()
    end)
    -- Vérification du premier script
    if success1 then
        print("Premier script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du premier script : ", error1)
    end

    -- Charger et exécuter le deuxième script
    local success2, error2 = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Denverrz/scripts/master/PRISONWARE_v1.3.txt"))()
    end)
    -- Vérification du deuxième script
    if success2 then
        print("Deuxième script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du deuxième script : ", error2)
    end

-- Bloxfruit

elseif placeId == 2753915549 then  -- ID de "Bloxfruit"
    -- Exécuter le script principal pour "Bloxfruit"
    print("Vous êtes dans Bloxfruit. Chargement des scripts...")

    -- Charger et exécuter le premier script
    local success1, error1 = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/REDzHUB/Bloxfruits/main/redz9999"))()
    end)
    -- Vérification du premier script
    if success1 then
        print("Premier script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du premier script : ", error1)
    end

-- Legends Of Speed

elseif placeId == 3101667897 then  -- ID de "Legends Of Speed"
    -- Exécuter le script principal pour "Legends Of Speed"
    print("Vous êtes dans Legends Of Speed. Chargement des scripts...")

    -- Charger et exécuter le premier script
    local success1, error1 = pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/WhackyCode/SPEEDDEVIL/main/legendsofspeed.lua"))()
    end)
    -- Vérification du premier script
    if success1 then
        print("Premier script exécuté avec succès.")
    else
        warn("Erreur lors de l'exécution du premier script : ", error1)
    end
end
